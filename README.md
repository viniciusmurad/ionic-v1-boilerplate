# ionicV1ModularBoilerplate

Clone the project using

``` git clone https://github.com/viniciusmurad/ionicV1ModularBoilerplate.git ```

Install dependencies using:

``` npm install ```

``` bower install ```

Run server using:

``` ionic serve ```
